# coding=utf-8
{{ code_model.options['license_header'] }}

{{ imports }}

# the sample just shows how to use the method and may not run directly
{% if origin_file %}
# x-ms-original-file: {{ origin_file }}
{% endif %}
def main():
    {{ annotation }}
    client = {{ code_model.client.name }}(
        {% for key, value in client_params.items() %}
        {{ key }}={{ value }},
        {% endfor %}
    )

    {% if operation_params %}
    result = client.{{ operation_group_name }}.{{ operation_name }}(
        {% for key, value in operation_params.items() %}
        {{ key }}={{ value }},
        {% endfor %}
    )
    {% else %}
    result = client.{{ operation_group_name }}.{{ operation_name }}()
    {% endif %}

if __name__ == "__main__":
    main()
